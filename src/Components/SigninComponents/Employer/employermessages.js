import React, { useEffect } from 'react'
import { useLocation } from 'react-router-dom';

function EmployerMessages() {
    const location=useLocation();
useEffect(()=>{
},[])
  return (
    <div>EmployerMessages</div>
  )
}

export default EmployerMessages